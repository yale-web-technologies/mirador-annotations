<!--<h2>Log in</h2>
Yale users should <%#= link_to "sign in with CAS",
                              omniauth_authorize_path(resource_name, 'cas') %>

<!--<%#= render "devise/shared/links" %>-->

<div class="container">
    <div class="page-header">
        <h2>Ten Thousand Rooms Sign in</h2>
    </div>
    <div class="col-sm-12 col-md-6">

        <div class="panel  panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title">Yale Users</h3>
            </div>
            <div class="panel-body">
                Yale users should <%= link_to "sign in with CAS", omniauth_authorize_path(resource_name, 'cas') %>.
            </div>
        </div>

        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Email-based accounts</h3>
            </div>
            <div class="panel-body">
                <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                    <div><%= f.label :email %><br/>
                        <%= f.email_field :email, autofocus: true %></div>

                    <div><%= f.label :password %><br/>
                        <%= f.password_field :password, autocomplete: "off" %></div>

                    <% if devise_mapping.rememberable? -%>
                        <div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
                    <% end -%>

                    <div><%= f.submit "Sign in" %></div>
                <% end %>
                <br/>
                <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
                    Need an account? <%= link_to "Sign up!", new_registration_path(resource_name) %><br />
                <% end -%>
            </div>
        </div>

        <div class="well">
            <%#= render "users/shared/links" %>
        </div>
    </div>


</div>
